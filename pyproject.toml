[project]
name = "weather-mcp-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "mcp",
  "httpx>=0.27.0",
  "fastapi>=0.110.0",
  "uvicorn[standard]>=0.30.0",
  "python-dotenv>=1.0.0"
]
